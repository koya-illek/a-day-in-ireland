# Optional local Worker/static-server key. Copy to .env and fill in.
# Production uses: npx wrangler secret put NTA_API_KEY --config wrangler.api.toml
NTA_API_KEY=
# Optional: OPW river bridge fallback for the non-Cloudflare adapter.
# Leave unset to keep the bridge disabled; production uses Browser Rendering instead.
# RIVER_BRIDGE_URL=
